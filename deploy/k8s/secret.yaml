apiVersion: v1
kind: Secret
metadata:
  name: content-loop-secrets
type: Opaque
stringData:
  DATABASE_URL: "contentloop:contentloop@tcp(mysql:3306)/content_loop?parseTime=true&charset=utf8mb4"
  FAMILYOS_API_KEY: "changeme"
  FAMILYOS_BASE_URL: "https://pattayadom.example.com"
  TELEGRAM_BOT_TOKEN: "changeme"
  TELEGRAM_CHANNEL_ID: "@changeme"
